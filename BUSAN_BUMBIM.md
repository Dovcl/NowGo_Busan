# 부산 붐빔 — 기능 설명

관광지 주변 도로의 **현재 통행 속도**를 같은 요일·시간대의 **평소 속도(baseline)** 와 비교해, 지금 그 주변이 평소보다 얼마나 원활하거나 막히는지 보여주는 기능.

## 1. 수집 데이터

**부산광역시 링크소통정보** (공공데이터포털, 도로 최소 구간 단위 실시간 속도·교통량, 약 9,200개 구간)

| 항목 | 내용 |
|---|---|
| 링크 ID | 도로 구간 식별자 |
| 도로명 · 시작/끝 지점 | 구간 위치 |
| 통행 속도(km/h) | 붐빔 계산의 핵심 값 |
| 교통량 | 구간별 함께 저장 |
| 기준 시각 | 원본이 데이터를 만든 정시 |

**좌표 매칭**: 링크 ID는 국토교통부 표준 노드·링크 체계와 같아서, 표준 노드링크의 형상(위경도)과 ID로 정확히 연결해 도로마다 좌표를 부여.

**보조 데이터**
- 구·군 요일별 방문객 비율 (한국관광공사, 통신 기반, 현지인·외지인·외국인 구분)
- 공휴일 정보 (공휴일은 일요일 패턴으로 비교)
- 행사 정보 (TourAPI · KOPIS · 다봄) — 근처 진행 중인 행사 표시

## 2. 수집 방식

- 원본은 정시 기준 1시간마다 갱신 → **Render 크론이 매시 10분**에 전체 93페이지 수집
- 저장 전 검증
  - 고유 구간 수 = 원본 전체 건수
  - 모든 페이지의 기준 시각 동일
  - 하나라도 어긋나면 그 회차는 저장하지 않고 다음 회차에 재시도
- 센서 오류 제외 (속도 0 이하, 150km/h 초과)
- 저장 위치 3곳
  - **최신 값**: 현재 속도 표시용
  - **최근 48시간 이력**: 오늘 그래프용
  - **baseline**: 도로 구간 × 요일 × 시간대(168칸)별 평소 속도, 수집할 때마다 누적 평균 갱신

## 3. 계산 방식

1. 관광지 좌표 반경 **500m** 안의 가까운 도로 구간 최대 **10개** 선택
2. 구간별로 `현재 속도 ÷ 평소 속도` 계산 (0~1)
   - 평소 속도 = 같은 구간의 같은 요일·같은 시간대 누적 평균 (3주 이상 관측된 구간)
   - 현재 속도가 관측된 시각의 요일·시간대와 맞춰 비교
   - 이번 관측을 제외한 직전 평균과 비교
3. 구간 값의 평균 = 붐빔 지수 `s_traffic` (1.0 = 평소와 같음, 0.5 = 평소의 절반 속도, 0.0 = 정체)
4. 표시용 **혼잡 지수** = `(1 − s_traffic) × 100`

**단계 표시**

| 화면 | 기준 | 단계 |
|---|---|---|
| 부산 붐빔 페이지 | 혼잡 지수 0~30 / 31~60 / 61~80 / 81~100 | 여유 · 보통 · 혼잡 · 매우 혼잡 |
| 홈 · 관광지 상세 | `s_traffic` 0.67 이상 / 0.33~0.67 / 0.33 미만 | 원활 · 보통 · 정체 |

**부가 표시**: 가장 느린 구간의 도로명(정체 구간), 근처에서 진행 중인 행사, 구·군 평균 대체 시 "지역 평균" 안내.

## 4. 화면 구성

**부산 붐빔 페이지**
- **지역 선택**: 16개 구·군 중 선택
- **대표 문구**: "OO 주변, 지금은 [한산한 / 보통인 / 붐비는 / 매우 붐비는] 편이에요" (최근 1시간 기준)
- **게이지**: 혼잡 지수를 반원 게이지 + 단계 색으로 표시
- **정보 카드 4개**
  - 평소보다 느림 (기준 속도 대비 %)
  - 현재 속도 (주변 도로 평균, km/h)
  - 평소 속도 (요일·시간 baseline, km/h)
  - 정체 구간 (가장 낮은 속도의 도로명)
- **오늘 실측 vs 평소 baseline 그래프**
  - 오늘 0시부터 지금까지 시간대별 **실측 속도(실선)** 와 같은 요일의 **평소 속도(점선)** 를 한 그래프에 겹쳐 표시
  - "지금" 시각 표시, 두 선이 벌어지는 시간대가 평소와 다른 붐빔/한산함
  - 매시 10분 갱신
- **지금 선택지**: 해당 지역 관광지를 붐빔 단계와 함께 나열, 눌러서 상세 확인

**홈 화면 카드**
- GPS 위치 주변 도로 혼잡을 원활 / 보통 / 정체로 표시하고 현재·평소 속도를 함께 표시
- 이안류는 GPS에서 가장 가까운 해수욕장(해운대·송정·임랑)의 위험도 표시
- GPS를 못 쓰면 부산시청 주변, 해운대 기본값

## 5. 데이터 흐름

```
링크소통정보 API (매시 갱신)
   → Render 크론 (매시 10분 수집·검증)
   → 최신 값 / 48시간 이력 / baseline(요일×시간) 저장
   → 관광지 반경 500m 도로 10개 → 현재 ÷ 평소 → 붐빔 지수
   → 홈 카드 · 부산 붐빔 페이지 · 관광지 상세
```
